{
  "name": "zenid-site",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-site.mjs",
    "test": "node --test scripts/*.test.mjs",
    "test:mcp": "npm test --workspace zenid-mcp"
  },
  "workspaces": [
    "packages/*"
  ]
}
